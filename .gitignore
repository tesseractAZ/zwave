# dependencies
node_modules/

# build artifacts
dist/
*.tsbuildinfo

# env / secrets — never commit
.env
.env.local
.env.production

# logs
*.log

# OS / editor
.DS_Store
.vscode/
.idea/

# local data (SQLite recorder + per-run scratch)
data/

# Claude session/scratch (kept local)
.claude/

# mutation-check.mjs crash-recovery sidecar (never commit; see scripts/mutation-check.mjs)
zwave_tui/server/.mutation-check-pending.json
